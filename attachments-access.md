# 附件存储与权限协作梳理

这块逻辑分成四层一起工作：附件记录保存在 `src/db/models/attachment.rs`，条目权限由 `src/db/models/cipher.rs` 统一判断，上传和删除入口在 `src/api/core/ciphers.rs`，本地文件下载入口在 `src/api/web.rs`。理解时抓住“先看条目权限，再看附件归属，最后才碰存储”的顺序，会比较清楚。

## 1. 附件到底存了什么

- 附件记录只保存 5 个核心字段：附件 id、所属条目 id、加密后的文件名、文件大小、附件密钥。
- 物理路径不带原始文件名，而是用“条目 id/附件 id”拼出来，所以文件定位完全依赖附件记录和所属条目。
- 生成下载地址时会先看当前存储后端：
  - 本地文件系统：返回站内 `/attachments/...` 地址，并额外签一个 5 分钟有效的下载 token。
  - 对象存储：直接返回预签名读取地址，下载不再经过本地文件路由。

这意味着附件的“元数据访问”和“真正把文件读出来”是两段式设计：先通过附件接口拿到地址，再按存储类型走本地 token 下载或对象存储预签名下载。

## 2. 上传时做了哪些校验

上传入口分两种，但都复用同一套保存逻辑：

- 旧接口是一把传完：请求里同时带文件和附件信息。
- v2 接口先建附件记录，再单独上传二进制内容。

真正落盘前会按下面顺序校验：

1. 条目必须存在。
2. 当前用户对这个条目必须有写权限。
3. 文件大小不能是负数，也不能把个人或组织的附件配额打爆。
4. v2 接口会把“之前已经预占的附件大小”补回去再算一次，避免同一个附件被重复计入配额。
5. v2 接口还会核对“声明大小”和“实际上传大小”，允许上下 1 MiB 浮动；如果超出范围，会把刚才建好的附件记录删掉再报错。
6. 旧接口额外要求 multipart 里真的带了文件名和附件密钥。

校验都过了以后，文件才会写入附件存储目录，并触发条目同步更新；如果附件属于组织，还会记一条“附件创建”事件。

这里有两个实现特点值得记住：

- 系统几乎不关心文件扩展名或内容类型，核心防线是“条目写权限 + 配额 + 大小一致性”。
- v2 接口先写库、后传文件，所以它专门补了一段“大小回补”和“大小对账”逻辑，目的就是防止预建记录把配额算乱。

## 3. 下载和权限怎么协作

下载也分成两步，不是直接开放存储路径：

### 3.1 先拿附件元数据

附件元数据接口会先检查三件事：

1. 条目存在。
2. 当前用户对条目至少有可读权限。
3. 这个附件确实挂在当前条目下面，而不是别的条目里的同名附件。

只有这三步都通过，接口才会把附件的下载地址返回给前端。

### 3.2 再按下载地址取文件

- 本地文件系统模式下，返回的是站内下载地址。真正读文件时，`src/api/web.rs` 只认下载 token，不再重新查登录态。
- 这个 token 里绑了条目 id 和附件 id，并且只有 5 分钟有效期，请求路径里的两个 id 必须和 token 里一致才会放行。
- 对象存储模式下，前一步已经给了预签名地址，后续下载依赖对象存储自己的签名校验。

所以附件下载的安全边界其实是“双门”：

- 第一门由业务接口负责，确认“这个用户能不能看到这个条目的附件”。
- 第二门由短期 token 或预签名地址负责，确认“这次真正下载的就是刚才放行的那个附件”。

## 4. 权限协作的核心规则

附件本身没有单独的一套权限模型，它完全跟着所属条目走。条目的访问限制统一从 `src/db/models/cipher.rs` 算出来，附件接口只是消费结果。

权限判定可以概括成下面几层：

- 条目直接归用户本人所有：默认可读可写。
- 用户在组织里是全量访问角色，或者所在组拥有全量访问：默认可读可写。
- 否则看条目所在集合给出的限制位：只读、隐藏密码、可管理。

同一个条目可能挂在多个集合下，合并方式不是简单取最大权限，而是：

- 只读：所有集合都只读，最终才只读。
- 隐藏密码：所有集合都隐藏，最终才隐藏。
- 管理：任意一个集合给了管理权，就算有管理权。

落到附件操作时，规则很直接：

- 查看附件信息、拿下载地址：只要条目对当前用户“可访问”即可。
- 上传附件、删除附件、删除条目：要求条目“可写”，也就是不是纯只读，或者虽然只读但带管理权。

这就是为什么附件接口里总是先找条目，再问条目权限，而不是自己单独算一遍。

## 5. 文件清理是怎么做的

### 5.1 删除单个附件

删除单个附件会先做三步校验：

1. 附件记录存在。
2. 附件确实属于当前条目。
3. 当前用户对条目有写权限。

通过后会进入统一删除逻辑：

- 先删数据库里的附件记录，删除 SQL 自带最多 10 次重试。
- 再删存储后端里的物理文件。
- 如果物理文件本来就没了，只记调试日志，不当成失败。
- 如果存储删除报其他错误，异常会往上抛。

这一段有个后果要特别注意：数据库记录先删、文件后删，所以一旦物理删除阶段报真正错误，接口会失败，但库里那条附件记录已经不在了，可能留下孤儿文件。这套实现明显优先保证“业务侧不要再看到脏附件记录”，而不是严格追求库和存储的事务一致性。

### 5.2 删除整个条目

硬删除条目时，清理顺序是：

1. 更新相关用户的同步版本。
2. 删除文件夹关联。
3. 删除集合关联。
4. 删除该条目下的全部附件。
5. 删除收藏关联。
6. 最后删条目本身。

附件批量删除没有走一条大 SQL，而是把每个附件逐个调用单附件删除逻辑。这样做的目的很明确：确保每个附件对应的物理文件也会一起删掉。

## 6. 条目删除后的影响

### 6.1 软删除

- 软删除只是在条目上打删除时间，不会动附件记录，也不会动物理文件。
- 这意味着附件内容还在，后续如果条目从回收站恢复，附件也能跟着恢复。
- 定时清理任务会扫描“删除时间超过保留期”的条目，再转成真正的硬删除。

### 6.2 硬删除

- 条目记录会被彻底删掉。
- 关联的附件记录会逐个删掉，物理文件也会尝试同步删除。
- 文件夹关联、集合关联、收藏关系也会一起清掉。
- 单条硬删除会发送“登录项已删除”的同步事件，让客户端把本地对应条目移除。

### 6.3 清空组织或个人保险库

- 清空组织保险库，本质上是遍历组织下所有条目逐个硬删除，因此附件也会跟着清干净。
- 清空个人保险库，同样是遍历本人拥有的条目逐个硬删除；附件会被删，个人文件夹也会另外清掉。

所以“条目删除后的影响”可以一句话总结：

- 软删除只藏起来，不动附件。
- 硬删除和批量清空都会顺着条目级联，把附件记录和物理文件一起清掉。

## 7. 一张心智图串起来

可以把整套逻辑理解成下面这条链：

1. 先用条目权限决定用户能不能碰附件。
2. 再用附件归属校验，确认附件确实属于这个条目。
3. 上传时重点防配额超限和 v2 大小对不上。
4. 下载时重点防 token 被拿去下载别的附件。
5. 删除时优先保证数据库别再暴露失效附件，再尽量把物理文件清掉。
6. 条目软删除保留附件，条目硬删除才真正触发附件清理。

## 8. 上传失败与清理残留的深度分析

这一节专门拆开四种容易混淆的状态：数据库记录先保存、文件写入失败、大小不一致删除、物理删除失败。理解它们之间的区别，才能判断哪些残留会真正影响后续操作。

### 8.1 四种失败场景的区别

#### 场景 A：v2 接口——数据库记录先保存，文件还没传
这是 v2 接口的正常中间状态，不是失败。

- 触发点：`POST /ciphers/<cipher_id>/attachment/v2` 成功返回，但 `POST /ciphers/<cipher_id>/attachment/<attachment_id>` 还没调用。
- 数据库状态：附件记录已存在，`file_size` 是客户端声明的大小。
- 物理文件状态：**完全不存在**。
- 对后续操作的影响：
  - 客户端拿到 `attachmentId` 后，可以继续上传，也可以中断后重试上传。
  - 客户端如果直接放弃，数据库里会留下一条“有记录、没文件”的悬空记录。
  - 用户在列表里能看到这个附件，点下载会成功拿到带 token 的地址，但真正读文件时会因为文件不存在而静默返回 404。
  - 这个附件会计入用户/组织的配额占用，因为 `size_by_user` 和 `size_by_org` 只扫数据库，不管文件真的在不在。

> 代码对应：[post_attachment_v2](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/api/core/ciphers.rs#L1125-L1164) 在第 1149 行先 `attachment.save()` 成功，立刻返回，不等待文件上传。

---

#### 场景 B：文件写入失败（数据库记录已存在，文件落盘失败）
这是上传过程中真正的错误。

- 触发点：`save_temp_file()` 抛出异常，比如磁盘满、权限不足、对象存储故障。
- 数据库状态：附件记录仍存在，`file_size` 可能是声明值，也可能已经更新为实际值。
- 物理文件状态：**可能部分写入，也可能完全没写**，取决于失败时机。
- 对后续操作的影响：
  - 接口向上返回错误，客户端知道上传失败了。
  - 但数据库里的附件记录**不会自动回滚**，因为代码在 `save_temp_file()` 之前就已经把记录写进去，或者已经把大小更新完了。
  - 用户侧表现和场景 A 一样：看得见附件记录，下载点进去是 404。
  - 配额同样被占用，因为记录还在。
  - 后续如果客户端重试上传同一个 attachmentId，会再次走 v2 数据上传接口，这时候会因为 `overwrite = true` 覆盖写入。

> 代码对应：[save_attachment](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/api/core/ciphers.rs#L1180-L1341) 中第 1285-1290 行更新记录后，第 1315 行才调 `save_temp_file()`，中间没有事务回滚保护。`save_temp_file()` 在 [util.rs#L878-L895](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/util.rs#L878-L895) 失败会直接向上抛错。

---

#### 场景 C：大小不一致——主动删除记录
这是 v2 接口专门设计的“对账失败回滚”。

- 触发点：客户端声明的大小和实际上传大小相差超过 ±1 MiB。
- 数据库状态：**先建的记录被主动删除**，调用的是完整的 `attachment.delete()`。
- 物理文件状态：因为还没走到 `save_temp_file()`，所以**根本没写**。
- 对后续操作的影响：
  - 数据库干净，不留记录。
  - 配额立刻释放，因为记录删了。
  - 客户端收到明确的“大小不匹配”错误，需要重新走一遍 v2 流程拿新的 attachmentId。
  - 这种设计是 v2 接口独有的容错，目的就是防止“声明很大、实际传很小”把配额骗走。

> 代码对应：[save_attachment](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/api/core/ciphers.rs#L1180-L1341) 第 1291-1294 行：大小超范围时，先 `attachment.delete(&conn).await.ok();` 再抛错，这里用 `ok()` 吞掉删除本身的错误，保证错误信息是“大小不匹配”而不是“删记录失败”。

---

#### 场景 D：物理删除失败——库里已删，文件还在
这是删除流程中最值得注意的半成功状态。

- 触发点：`attachment.delete()` 里数据库删成功了，但 `operator.delete()` 返回非 NotFound 的错误。
- 数据库状态：**附件记录已彻底消失**，10 次重试都没拦住删成功。
- 物理文件状态：**仍然留在磁盘/对象存储里**，变成孤儿文件。
- 对后续操作的影响：
  - 接口向上返回删除失败，调用方知道出错了。
  - 但数据库这边已经不可逆地删掉了，因为数据库删除在最前面，而且代码里没有“删文件失败就把记录插回去”的补偿逻辑。
  - 用户侧看不到这个附件了，列表里不显示，因为列表是从数据库读的。
  - 配额立刻释放，因为记录没了。
  - 但物理文件会一直占着存储空间，直到有人手动清理，或者未来加了孤儿文件扫描任务。
  - 客户端再次上传同名附件（其实是新 attachmentId）不会冲突，因为路径用的是新 id。

> 代码对应：[Attachment::delete](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/db/models/attachment.rs#L114-L137) 第 123 行数据库删除成功后，第 128-133 行才处理文件删除；非 NotFound 错误直接 `return Err(e.into())`，但数据库那边已经提交了。

### 8.2 四种场景对比表

| 场景 | 数据库记录 | 物理文件 | 配额占用 | 用户可见 | 下载行为 | 可否重试 |
|------|-----------|----------|----------|----------|----------|----------|
| A. v2 先存记录待上传 | ✅ 存在 | ❌ 不存在 | ✅ 占用 | ✅ 可见 | 点下载返回 404 | ✅ 可继续传同 ID |
| B. 文件落盘失败 | ✅ 残留 | ⚠️ 可能部分写入 | ✅ 占用 | ✅ 可见 | 点下载返回 404 | ⚠️ 再传会覆盖 |
| C. 大小不一致回滚 | ❌ 已删 | ❌ 不存在 | ❌ 释放 | ❌ 不可见 | —— | ❌ 需重新拿 ID |
| D. 物理删除失败 | ❌ 已删 | ✅ 残留 | ❌ 释放 | ❌ 不可见 | —— | —— |

### 8.3 哪些残留会真正影响后续操作

从代码实际路径来看，真正会带来长期影响的是下面这几种残留：

#### 影响一：“有记录没文件”的悬空记录（场景 A + 场景 B）
- **对下载的影响**：用户正常点下载，能拿到元数据，能拿到带 token 的 URL，token 校验也过，但最后 `NamedFile::open()` 失败静默返回 None，前端表现就是下载按钮点了没反应，或者直接跳 404。
  - 代码路径：[web.rs#L197-L207](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/api/web.rs#L197-L207) 第 206 行 `.await.ok()` 把打开失败直接吞成 None，Rocket 会当成路由不匹配。
- **对配额的影响**：`Attachment::size_by_user()` 和 `size_by_org()` 都是纯 SQL 求和，只看 `file_size` 字段，不检查文件存在性，所以悬空记录会持续占用配额，直到用户手动删这个附件。
  - 代码路径：[attachment.rs#L161-L177](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/db/models/attachment.rs#L161-L177) 和 [attachment.rs#L191-L207](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/db/models/attachment.rs#L191-L207) 都是 `sum(file_size)`，没有文件存在性校验。
- **对清理的影响**：用户点删除时，`Attachment::delete()` 会正常删掉数据库记录，然后尝试删文件时遇到 NotFound 会打 debug 日志但不报错误，所以用户侧表现就是“删成功了”，没有感知。
  - 代码路径：[attachment.rs#L128-L131](file:///d:/fz/0601/solo-dogfeeding/code/6-vaultwarden/src/db/models/attachment.rs#L128-L131) NotFound 只记日志，不往上抛。

#### 影响二：“库里已删、文件残留”的孤儿文件（场景 D）
- **对下载的影响**：完全没影响，因为用户根本看不到这个附件，也拿不到下载地址。
- **对配额的影响**：没有影响，因为数据库记录已经删了，配额计算不扫磁盘。
- **对清理的影响**：
  - 正常的删除流程不会再碰到它，因为没有数据库记录指向它。
  - 条目硬删除也不会清到它，因为 `delete_all_by_cipher()` 是先查数据库里属于这个条目的附件，再逐个删，孤儿文件不在这个列表里。
  - 回收站自动清理也碰不到，因为 `purge_trash()` 同样走条目级联。
  - **结论**：这类孤儿文件目前没有自动清理机制，只能靠外部脚本扫描目录，对比数据库里的附件路径，找出没引用的文件删掉。

#### 影响三：v2 接口重试上传的覆盖行为
- 客户端如果重试上传同一个 `attachmentId`，`save_temp_file()` 的 `overwrite = true` 会直接覆盖写入，不会报错。
- 这是正确的行为，能支撑断点续传和重试场景。
- 但如果前一次上传已经写了一部分文件，后续覆盖是完全替换，不会增量追加，所以部分写入的残片会被覆盖掉，不会留半截文件。

### 8.4 清理流程的状态传播

当条目被删除时，附件清理会按下面的顺序传播状态：

```
条目硬删除
    ↓
调用 Attachment::delete_all_by_cipher()  [attachment.rs#L139-L144]
    ↓
遍历每个附件记录
    ↓
对每个附件调用 Attachment::delete()        [attachment.rs#L114-L137]
    ├─ 先删数据库记录（10次重试）
    │   └─ 成功 → 记录消失，配额释放
    │   └─ 失败 → 整个删除失败，记录还在
    ↓
再删物理文件
    ├─ 成功 → 文件清理完成
    ├─ NotFound → 打 debug 日志，当成功（接受“文件本来就没了”）
    └─ 其他错误 → 向上抛错，但数据库记录已经没了 → 产生孤儿文件
```

关键点在于：**只要数据库删除那步过了，后面文件删没删，业务层都当这个附件不存在了**。这是一种“数据库优先”的一致性模型，代价是可能产生存储垃圾。

### 8.5 现有设计的补偿机制与缺口

目前代码里对残留的处理很有限：

- **有的补偿**：
  - 大小不匹配时主动回滚数据库记录（场景 C）。
  - 文件不存在时不报错，允许删除继续（接受悬空记录被清理）。
  - 数据库删除带 10 次重试，减少并发冲突导致的删库失败。

- **没的补偿**：
  - 文件落盘失败时不回滚数据库记录（场景 B 会留悬空记录）。
  - 物理删除失败时不把数据库记录插回去（场景 D 会留孤儿文件）。
  - 没有后台任务扫描“有记录没文件”或“有文件没记录”的不一致。
  - 没有对外暴露的清理接口，管理员只能手动比对数据库和目录。

这就是为什么上传失败和删除失败的残留，在用户感知上有的看得到、有的看不到，但系统本身没有全自动修复。如果要做一致性治理，核心方向就是加两个扫描：一是扫数据库找文件不存在的记录，二是扫存储目录找数据库里没引用的文件。

## 9. 一张心智图串起来

可以把整套逻辑理解成下面这条链：

1. 先用条目权限决定用户能不能碰附件。
2. 再用附件归属校验，确认附件确实属于这个条目。
3. 上传时重点防配额超限和 v2 大小对不上。
4. 下载时重点防 token 被拿去下载别的附件。
5. 删除时优先保证数据库别再暴露失效附件，再尽量把物理文件清掉。
6. 条目软删除保留附件，条目硬删除才真正触发附件清理。

如果后面还要继续深挖，最值得看的就是两个边界：

- 本地文件下载为什么只校验 token、不回查登录态。
- 物理文件删除失败时为什么接受“库里已删、文件残留”的状态，以及后续有没有补偿清理机制。
