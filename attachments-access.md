# 附件存储与权限协作梳理

这块逻辑分成四层一起工作：附件记录保存在 `src/db/models/attachment.rs`，条目权限由 `src/db/models/cipher.rs` 统一判断，上传和删除入口在 `src/api/core/ciphers.rs`，本地文件下载入口在 `src/api/web.rs`。理解时抓住"先看条目权限，再看附件归属，最后才碰存储"的顺序，会比较清楚。

## 1. 附件到底存了什么

- 附件记录只保存 5 个核心字段：附件 id、所属条目 id、加密后的文件名、文件大小、附件密钥。
- 物理路径不带原始文件名，而是用"条目 id/附件 id"拼出来，所以文件定位完全依赖附件记录和所属条目。
- 生成下载地址时会先看当前存储后端：
  - 本地文件系统：返回站内 `/attachments/...` 地址，并额外签一个 5 分钟有效的下载 token。
  - 对象存储：直接返回预签名读取地址，下载不再经过本地文件路由。

这意味着附件的"元数据访问"和"真正把文件读出来"是两段式设计：先通过附件接口拿到地址，再按存储类型走本地 token 下载或对象存储预签名下载。

## 2. 上传时做了哪些校验

上传入口分两种，但都复用同一套保存逻辑：

- 旧接口是一把传完：请求里同时带文件和附件信息。
- v2 接口先建附件记录，再单独上传二进制内容。

真正落盘前会按下面顺序校验：

1. 条目必须存在。
2. 当前用户对这个条目必须有写权限。
3. 文件大小不能是负数，也不能把个人或组织的附件配额打爆。
4. v2 接口会把"之前已经预占的附件大小"补回去再算一次，避免同一个附件被重复计入配额。
5. v2 接口还会核对"声明大小"和"实际上传大小"，允许上下 1 MiB 浮动；如果超出范围，会把刚才建好的附件记录删掉再报错。
6. 旧接口额外要求 multipart 里真的带了文件名和附件密钥。

校验都过了以后，文件才会写入附件存储目录，并触发条目同步更新；如果附件属于组织，还会记一条"附件创建"事件。

这里有两个实现特点值得记住：

- 系统几乎不关心文件扩展名或内容类型，核心防线是"条目写权限 + 配额 + 大小一致性"。
- v2 接口先写库、后传文件，所以它专门补了一段"大小回补"和"大小对账"逻辑，目的就是防止预建记录把配额算乱。

## 2b. 两个上传入口的校验时序差异

上面第 2 节把校验列成了"一套顺序"，但实际上 v2 的建记录阶段和真正上传数据阶段是分开的，校验也分布在两个阶段。这就导致同一个限制条件在两个入口里的触发时机不一样，甚至会产生"记录已经写进去了但限制还没拦住"的中间状态。

### v2 建记录阶段（`post_attachment_v2`）只做了最轻量的检查

`src/api/core/ciphers.rs` → `post_attachment_v2` 函数里的校验只有三步：

1. 条目存在。
2. 当前用户对条目有写权限。
3. 客户端声明的 `file_size` 不是负数。

**它不检查以下两项**：

- **配额是否已满**：`CONFIG.user_attachment_limit()` 和 `CONFIG.org_attachment_limit()` 的判断逻辑根本不在 `post_attachment_v2` 里，而是在后续 `save_attachment` 中才执行。
- **附件功能是否被禁用**：配额设为 `0` 表示"禁用附件上传"，对应的 `err!("Attachments are disabled")` 也只在 `save_attachment` 里触发。

这意味着：即使管理员已经把个人附件配额设成 0（禁用），或者用户的附件存储已经用满了，`post_attachment_v2` 依然会成功创建附件记录并返回 `attachmentId`。只有当客户端拿着这个 ID 去上传真正的文件数据时，`save_attachment` 才会拦住并返回错误。

### 真正上传数据阶段（`save_attachment`）才做完整校验

`src/api/core/ciphers.rs` → `save_attachment` 函数的校验顺序：

1. 实际上传数据的大小不是负数。
2. 条目存在。
3. 当前用户对条目有写权限。
4. **配额校验**：根据条目归属查个人或组织的附件限制。
   - 限制为 `Some(0)` → 返回"Attachments are disabled"。
   - 限制为 `Some(limit_kb)` → 计算已用空间和剩余空间。
   - 无限制 `None` → 跳过。
5. **单文件大小校验**：实际大小不能超过剩余配额。
6. v2 专属：大小对账（±1 MiB），对不上则删记录再报错。
7. Legacy 专属：multipart 里必须带文件名和附件密钥。

对于 v2 调用，`save_attachment` 还会把预建记录里已经占掉的 `file_size` 加回剩余空间（`size_adjust`），避免同一次上传被双重计入配额。

### 校验时序差异带来的中间状态

| 检查项 | v2 建记录时 | v2 上传数据时 | Legacy 上传时 |
|--------|------------|-------------|-------------|
| 条目存在 | ✅ | ✅ | ✅ |
| 写权限 | ✅ | ✅ | ✅ |
| file_size 非负 | ✅（声明值） | ✅（实际值） | ✅（实际值） |
| 附件功能禁用（limit=0） | ❌ | ✅ | ✅ |
| 配额剩余空间 | ❌ | ✅ | ✅ |
| 单文件超限 | ❌ | ✅ | ✅ |
| 大小对账 ±1MiB | ❌ | ✅ | —— |
| 文件名和密钥必填 | ❌ | —— | ✅ |

这张表直接解释了第 8 节场景 A 的根源：**v2 建记录时不拦配额和禁用，所以即使配额满了或附件功能被关了，数据库里照样会多出一条悬空记录**，直到客户端真正上传数据时才会被拦住。

## 2c. Legacy 和 v2 写文件失败后的行为差异

两个入口虽然都走 `save_attachment` 的统一落盘逻辑，但因为记录创建时机不同，写文件失败后表现也不一样。

### 记录残留与重试方式

**Legacy（`post_attachment`）**：

- 附件记录在 `save_attachment` 内部创建，时机在配额校验通过之后、`save_temp_file()` 之前。
- 如果 `save_temp_file()` 失败，记录已经写进数据库了，但没有回滚。
- Legacy 接口不接受 `attachmentId` 参数，每次调用都会重新生成一个新的 ID。所以**客户端没法用同一个 ID 重试**，只能重新上传，而重新上传会再建一条新记录，旧记录变成悬空残留。
- 结果：连续重试 N 次失败，数据库里就会有 N 条悬空记录，配额被 N 倍占用。

**v2（`post_attachment_v2` + `post_attachment_v2_data`）**：

- 附件记录在第一步 `post_attachment_v2` 就建好了。
- 第二步 `post_attachment_v2_data` 找到已有记录，传入 `save_attachment(Some(attachment), ...)`。
- 如果 `save_temp_file()` 失败，记录同样留在数据库里，但客户端**可以用同一个 `attachmentId` 再次调用 `post_attachment_v2_data`**。
- `save_temp_file()` 的 `overwrite = true` 会把上次的残片覆盖掉，所以重试不会产生新的悬空记录，也不会重复占用配额。
- 结果：不管重试多少次，始终只有一条记录，配额占用稳定。

### 同步通知的时机差异

`save_attachment` 里，同步通知和事件日志都在 `save_temp_file()` **之后**才执行：

```
save_temp_file() 成功
    ↓
nt.send_cipher_update(UpdateType::SyncCipherUpdate, ...)   ← 同步通知
    ↓
if 条目属于组织 { log_event(CipherAttachmentCreated, ...) }  ← 事件日志
```

这意味着**文件写入失败时，同步通知和事件日志都不会触发**。但两个入口在用户感知上还是有区别：

- **Legacy**：整个 `post_attachment` 调用失败，客户端收到错误响应。此时数据库里已有附件记录，但客户端不知道这个 ID（因为是服务端生成的），所以下次同步时才会看到一个"没有对应文件"的附件突然出现。
- **v2**：`post_attachment_v2` 第一步已经成功返回，客户端已经收到了包含新附件信息的 cipher JSON（响应体里的 `cipherResponse` / `cipherMiniResponse` 字段），并且 `post_attachment_v2` 里没有触发同步通知。第二步 `post_attachment_v2_data` 失败时，也不会触发同步通知。所以客户端本地已经有了这个附件的元数据，但服务端没有发出过同步推送——其他设备在下次全量同步前看不到这个附件。

### 事件日志的差异

- **Legacy**：如果文件写入失败，`log_event` 不会执行，审计日志里没有"附件创建"记录。但数据库里确实多了一条附件记录，从审计角度看是"有记录无日志"的不一致。
- **v2**：同样，文件写入失败时不记日志。但 v2 还有一种特殊情况——客户端如果建了记录后直接放弃上传，那连 `save_attachment` 都没进过，当然也没有事件日志，但数据库里已经有一条附件记录了。这种"建了记录但从未真正上传"的状态，审计日志完全体现不出来。

### 差异对比表

| 维度 | Legacy | v2 |
|------|--------|-----|
| 记录创建时机 | `save_attachment` 内部，配额校验之后 | `post_attachment_v2` 独立阶段，配额校验之前 |
| 写文件失败后记录残留 | ✅ 残留，且客户端不知道 ID | ✅ 残留，客户端知道 ID |
| 能否用同一 ID 重试 | ❌ 不能，每次重试生成新 ID | ✅ 可以，重试覆盖同一个 ID |
| 重试是否会额外占用配额 | ✅ 每次重试多一条悬空记录 | ❌ 始终只有一条记录 |
| 同步通知触发条件 | 文件写入成功后才发 | 同左 |
| 文件写入失败时客户端状态 | 不知道有新附件（下次同步才发现悬空记录） | 已经知道有新附件（v2 响应已返回元数据） |
| 审计日志 | 仅写入成功后记录 | 仅写入成功后记录；建记录后放弃上传则无日志 |

## 3. 下载和权限怎么协作

下载也分成两步，不是直接开放存储路径：

### 3.1 先拿附件元数据

附件元数据接口会先检查三件事：

1. 条目存在。
2. 当前用户对条目至少有可读权限。
3. 这个附件确实挂在当前条目下面，而不是别的条目里的同名附件。

只有这三步都通过，接口才会把附件的下载地址返回给前端。

### 3.2 再按下载地址取文件

- 本地文件系统模式下，返回的是站内下载地址。真正读文件时，`src/api/web.rs` 只认下载 token，不再重新查登录态。
- 这个 token 里绑了条目 id 和附件 id，并且只有 5 分钟有效期，请求路径里的两个 id 必须和 token 里一致才会放行。
- 对象存储模式下，前一步已经给了预签名地址，后续下载依赖对象存储自己的签名校验。

所以附件下载的安全边界其实是"双门"：

- 第一门由业务接口负责，确认"这个用户能不能看到这个条目的附件"。
- 第二门由短期 token 或预签名地址负责，确认"这次真正下载的就是刚才放行的那个附件"。

## 4. 权限协作的核心规则

附件本身没有单独的一套权限模型，它完全跟着所属条目走。条目的访问限制统一从 `src/db/models/cipher.rs` 算出来，附件接口只是消费结果。

权限判定可以概括成下面几层：

- 条目直接归用户本人所有：默认可读可写。
- 用户在组织里是全量访问角色，或者所在组拥有全量访问：默认可读可写。
- 否则看条目所在集合给出的限制位：只读、隐藏密码、可管理。

同一个条目可能挂在多个集合下，合并方式不是简单取最大权限，而是：

- 只读：所有集合都只读，最终才只读。
- 隐藏密码：所有集合都隐藏，最终才隐藏。
- 管理：任意一个集合给了管理权，就算有管理权。

落到附件操作时，规则很直接：

- 查看附件信息、拿下载地址：只要条目对当前用户"可访问"即可。
- 上传附件、删除附件、删除条目：要求条目"可写"，也就是不是纯只读，或者虽然只读但带管理权。

这就是为什么附件接口里总是先找条目，再问条目权限，而不是自己单独算一遍。

## 5. 文件清理是怎么做的

### 5.1 删除单个附件

删除单个附件会先做三步校验：

1. 附件记录存在。
2. 附件确实属于当前条目。
3. 当前用户对条目有写权限。

通过后会进入统一删除逻辑（`src/db/models/attachment.rs` → `Attachment::delete`）：

- 先删数据库里的附件记录，删除 SQL 自带最多 10 次重试。
- 再删存储后端里的物理文件。
- 如果物理文件本来就没了，只记调试日志，不当成失败。
- 如果存储删除报其他错误，异常会往上抛。

这一段有个后果要特别注意：数据库记录先删、文件后删，所以一旦物理删除阶段报真正错误，接口会失败，但库里那条附件记录已经不在了，可能留下孤儿文件。这套实现明显优先保证"业务侧不要再看到脏附件记录"，而不是严格追求库和存储的事务一致性。

### 5.2 删除整个条目

硬删除条目时（`src/db/models/cipher.rs` → `Cipher::delete`），清理顺序是：

1. 更新相关用户的同步版本。
2. 删除文件夹关联。
3. 删除集合关联。
4. 删除该条目下的全部附件。
5. 删除收藏关联。
6. 最后删条目本身。

附件批量删除没有走一条大 SQL，而是把每个附件逐个调用单附件删除逻辑。这样做的目的很明确：确保每个附件对应的物理文件也会一起删掉。

## 6. 条目删除后的影响

### 6.1 软删除

- 软删除只是在条目上打删除时间，不会动附件记录，也不会动物理文件。
- 这意味着附件内容还在，后续如果条目从回收站恢复，附件也能跟着恢复。
- 定时清理任务会扫描"删除时间超过保留期"的条目，再转成真正的硬删除。

### 6.2 硬删除

- 条目记录会被彻底删掉。
- 关联的附件记录会逐个删掉，物理文件也会尝试同步删除。
- 文件夹关联、集合关联、收藏关系也会一起清掉。
- 单条硬删除会发送"登录项已删除"的同步事件，让客户端把本地对应条目移除。

### 6.3 清空组织或个人保险库

- 清空组织保险库，本质上是遍历组织下所有条目逐个硬删除，因此附件也会跟着清干净。
- 清空个人保险库，同样是遍历本人拥有的条目逐个硬删除；附件会被删，个人文件夹也会另外清掉。

所以"条目删除后的影响"可以一句话总结：

- 软删除只藏起来，不动附件。
- 硬删除和批量清空都会顺着条目级联，把附件记录和物理文件一起清掉。

## 7. 一张心智图串起来

可以把整套逻辑理解成下面这条链：

1. 先用条目权限决定用户能不能碰附件。
2. 再用附件归属校验，确认附件确实属于这个条目。
3. 上传时重点防配额超限和 v2 大小对不上。
4. 下载时重点防 token 被拿去下载别的附件。
5. 删除时优先保证数据库别再暴露失效附件，再尽量把物理文件清掉。
6. 条目软删除保留附件，条目硬删除才真正触发附件清理。

## 8. 上传失败与清理残留的深度分析

这一节专门拆开四种容易混淆的状态：数据库记录先保存、文件写入失败、大小不一致删除、物理删除失败。理解它们之间的区别，才能判断哪些残留会真正影响后续操作。

### 8.1 四种失败场景的区别

#### 场景 A：v2 接口——数据库记录先保存，文件还没传
这是 v2 接口的正常中间状态，不是失败。

- 触发点：`POST /ciphers/<cipher_id>/attachment/v2` 成功返回，但 `POST /ciphers/<cipher_id>/attachment/<attachment_id>` 还没调用。
- 数据库状态：附件记录已存在，`file_size` 是客户端声明的大小。
- 物理文件状态：**完全不存在**。
- 对后续操作的影响：
  - 客户端拿到 `attachmentId` 后，可以继续上传，也可以中断后重试上传。
  - 客户端如果直接放弃，数据库里会留下一条"有记录、没文件"的悬空记录。
  - 用户在列表里能看到这个附件，点下载会成功拿到带 token 的地址，但真正读文件时会因为文件不存在而静默返回 404。
  - 这个附件会计入用户/组织的配额占用，因为 `size_by_user` 和 `size_by_org` 只扫数据库，不管文件真的在不在。

> 代码对应：`src/api/core/ciphers.rs` → `post_attachment_v2` 函数中，先执行 `attachment.save()` 成功后立刻返回响应，不等待文件上传。此时附件记录已在库中，但物理文件尚未写入。

---

#### 场景 B：文件写入失败（数据库记录已存在，文件落盘失败）
这是上传过程中真正的错误。

- 触发点：`save_temp_file()` 抛出异常，比如磁盘满、权限不足、对象存储故障。
- 数据库状态：附件记录仍存在，`file_size` 可能是声明值，也可能已经更新为实际值。
- 物理文件状态：**可能部分写入，也可能完全没写**，取决于失败时机。
- 对后续操作的影响：
  - 接口向上返回错误，客户端知道上传失败了。
  - 但数据库里的附件记录**不会自动回滚**，因为代码在 `save_temp_file()` 之前就已经把记录写进去，或者已经把大小更新完了。
  - 用户侧表现和场景 A 一样：看得见附件记录，下载点进去是 404。
  - 配额同样被占用，因为记录还在。
  - 后续如果客户端重试上传同一个 attachmentId，会再次走 v2 数据上传接口，这时候会因为 `overwrite = true` 覆盖写入。

> 代码对应：`src/api/core/ciphers.rs` → `save_attachment` 函数中，v2 分支先更新附件记录的 `file_size` 并 `attachment.save()`，然后才调用 `save_temp_file()`，中间没有事务回滚保护。`save_temp_file()` 定义在 `src/util.rs`，失败时直接向上抛错，不做任何数据库回退。

---

#### 场景 C：大小不一致——主动删除记录
这是 v2 接口专门设计的"对账失败回滚"。

- 触发点：客户端声明的大小和实际上传大小相差超过 ±1 MiB。
- 数据库状态：**先建的记录被主动删除**，调用的是完整的 `attachment.delete()`。
- 物理文件状态：因为还没走到 `save_temp_file()`，所以**根本没写**。
- 对后续操作的影响：
  - 数据库干净，不留记录。
  - 配额立刻释放，因为记录删了。
  - 客户端收到明确的"大小不匹配"错误，需要重新走一遍 v2 流程拿新的 attachmentId。
  - 这种设计是 v2 接口独有的容错，目的就是防止"声明很大、实际传很小"把配额骗走。

> 代码对应：`src/api/core/ciphers.rs` → `save_attachment` 函数中，大小超出 `[file_size - 1MiB, file_size + 1MiB]` 范围时，先调用 `attachment.delete(&conn).await.ok()` 主动删除记录，再抛"大小不匹配"错误。这里用 `ok()` 吞掉删除本身的错误，保证错误信息是"大小不匹配"而不是"删记录失败"。

---

#### 场景 D：物理删除失败——库里已删，文件还在
这是删除流程中最值得注意的半成功状态。

- 触发点：`attachment.delete()` 里数据库删成功了，但 `operator.delete()` 返回非 NotFound 的错误。
- 数据库状态：**附件记录已彻底消失**，10 次重试都没拦住删成功。
- 物理文件状态：**仍然留在磁盘/对象存储里**，变成孤儿文件。
- 对后续操作的影响：
  - 接口向上返回删除失败，调用方知道出错了。
  - 但数据库这边已经不可逆地删掉了，因为数据库删除在最前面，而且代码里没有"删文件失败就把记录插回去"的补偿逻辑。
  - 用户侧看不到这个附件了，列表里不显示，因为列表是从数据库读的。
  - 配额立刻释放，因为记录没了。
  - 但物理文件会一直占着存储空间，直到有人手动清理，或者未来加了孤儿文件扫描任务。
  - 客户端再次上传同名附件（其实是新 attachmentId）不会冲突，因为路径用的是新 id。

> 代码对应：`src/db/models/attachment.rs` → `Attachment::delete` 方法中，数据库删除成功后才执行 `operator.delete(&file_path)`；如果文件删除返回非 NotFound 错误，直接 `return Err(e.into())`，但此时数据库记录已经提交删除，无法恢复。

### 8.2 四种场景对比表

| 场景 | 数据库记录 | 物理文件 | 配额占用 | 用户可见 | 下载行为 | 可否重试 |
|------|-----------|----------|----------|----------|----------|----------|
| A. v2 先存记录待上传 | ✅ 存在 | ❌ 不存在 | ✅ 占用 | ✅ 可见 | 点下载返回 404 | ✅ 可继续传同 ID |
| B. 文件落盘失败 | ✅ 残留 | ⚠️ 可能部分写入 | ✅ 占用 | ✅ 可见 | 点下载返回 404 | ⚠️ 再传会覆盖 |
| C. 大小不一致回滚 | ❌ 已删 | ❌ 不存在 | ❌ 释放 | ❌ 不可见 | —— | ❌ 需重新拿 ID |
| D. 物理删除失败 | ❌ 已删 | ✅ 残留 | ❌ 释放 | ❌ 不可见 | —— | —— |

### 8.3 哪些残留会真正影响后续操作

从代码实际路径来看，真正会带来长期影响的是下面这几种残留：

#### 影响一："有记录没文件"的悬空记录（场景 A + 场景 B）
- **对下载的影响**：用户正常点下载，能拿到元数据，能拿到带 token 的 URL，token 校验也过，但最后 `NamedFile::open()` 失败静默返回 None，前端表现就是下载按钮点了没反应，或者直接跳 404。
  - 代码位置：`src/api/web.rs` → `attachments` 路由处理函数中，`NamedFile::open(...).await.ok()` 把打开失败直接吞成 None，Rocket 会当成路由不匹配。
- **对配额的影响**：`Attachment::size_by_user()` 和 `size_by_org()` 都是纯 SQL 求和，只看 `file_size` 字段，不检查文件存在性，所以悬空记录会持续占用配额，直到用户手动删这个附件。
  - 代码位置：`src/db/models/attachment.rs` → `size_by_user` 和 `size_by_org` 方法，均使用 `sum(attachments::file_size)` 查询，没有文件存在性校验。
- **对清理的影响**：用户点删除时，`Attachment::delete()` 会正常删掉数据库记录，然后尝试删文件时遇到 NotFound 会打 debug 日志但不报错误，所以用户侧表现就是"删成功了"，没有感知。
  - 代码位置：`src/db/models/attachment.rs` → `Attachment::delete` 方法中，`opendal::ErrorKind::NotFound` 分支只记 `debug!()` 日志，不往上抛错。

#### 影响二："库里已删、文件残留"的孤儿文件（场景 D）
- **对下载的影响**：完全没影响，因为用户根本看不到这个附件，也拿不到下载地址。
- **对配额的影响**：没有影响，因为数据库记录已经删了，配额计算不扫磁盘。
- **对清理的影响**：
  - 正常的删除流程不会再碰到它，因为没有数据库记录指向它。
  - 条目硬删除也不会清到它，因为 `delete_all_by_cipher()` 是先查数据库里属于这个条目的附件，再逐个删，孤儿文件不在这个列表里。
  - 回收站自动清理也碰不到，因为 `purge_trash()` 同样走条目级联。
  - **结论**：这类孤儿文件目前没有自动清理机制，只能靠外部脚本扫描目录，对比数据库里的附件路径，找出没引用的文件删掉。

#### 影响三：v2 接口重试上传的覆盖行为
- 客户端如果重试上传同一个 `attachmentId`，`save_temp_file()` 的 `overwrite = true` 会直接覆盖写入，不会报错。
- 这是正确的行为，能支撑断点续传和重试场景。
- 但如果前一次上传已经写了一部分文件，后续覆盖是完全替换，不会增量追加，所以部分写入的残片会被覆盖掉，不会留半截文件。

### 8.4 清理流程的状态传播

当条目被删除时，附件清理会按下面的顺序传播状态：

```
条目硬删除
    ↓
调用 Attachment::delete_all_by_cipher()   （src/db/models/attachment.rs）
    ↓
遍历每个附件记录
    ↓
对每个附件调用 Attachment::delete()       （src/db/models/attachment.rs）
    ├─ 先删数据库记录（10次重试）
    │   └─ 成功 → 记录消失，配额释放
    │   └─ 失败 → 整个删除失败，记录还在
    ↓
再删物理文件
    ├─ 成功 → 文件清理完成
    ├─ NotFound → 打 debug 日志，当成功（接受"文件本来就没了"）
    └─ 其他错误 → 向上抛错，但数据库记录已经没了 → 产生孤儿文件
```

关键点在于：**只要数据库删除那步过了，后面文件删没删，业务层都当这个附件不存在了**。这是一种"数据库优先"的一致性模型，代价是可能产生存储垃圾。

### 8.5 现有设计的补偿机制与缺口

目前代码里对残留的处理很有限：

- **有的补偿**：
  - 大小不匹配时主动回滚数据库记录（场景 C）。
  - 文件不存在时不报错，允许删除继续（接受悬空记录被清理）。
  - 数据库删除带 10 次重试，减少并发冲突导致的删库失败。

- **没的补偿**：
  - 文件落盘失败时不回滚数据库记录（场景 B 会留悬空记录）。
  - 物理删除失败时不把数据库记录插回去（场景 D 会留孤儿文件）。
  - 没有后台任务扫描"有记录没文件"或"有文件没记录"的不一致。
  - 没有对外暴露的清理接口，管理员只能手动比对数据库和目录。

这就是为什么上传失败和删除失败的残留，在用户感知上有的看得到、有的看不到，但系统本身没有全自动修复。如果要做一致性治理，核心方向就是加两个扫描：一是扫数据库找文件不存在的记录，二是扫存储目录找数据库里没引用的文件。

## 8b. 本地存储与对象存储的残留差异

第 8 节分析的所有失败场景，在数据库层面两种存储后端完全一致——因为数据库操作和存储操作是解耦的。差异集中在"缺文件时的下载失败路径""写入残留的形态"和"孤儿文件的外部清理方式"这三个地方。

### 8b.1 下载失败的位置和错误表现

下载地址的生成在 `src/db/models/attachment.rs` → `Attachment::get_url`，会先判断当前 operator 是不是本地文件系统（`src/storage.rs` → `is_fs_operator`，判断 scheme 是否为 `FS_SCHEME`）。两种后端的下载路径完全不同：

#### 本地文件系统：站内 token 下载

```
客户端拿元数据 → get_url() 返回 /attachments/{cipher_id}/{file_id}?token=xxx
    ↓
客户端请求站内地址 → src/api/web.rs → attachments() 路由
    ↓
校验 token（JWT 签名 + 有效期 + sub/file_id 绑定）
    ↓
NamedFile::open(本地路径) → 成功返回文件 / 失败返回 None
```

**缺文件时的表现**：

- `NamedFile::open()` 失败，`.ok()` 把错误吞成 `None`。
- Rocket 把 `None` 当作"路由不匹配"，返回 HTTP 404。
- 客户端看到的不是 Vaultwarden 自己的错误消息，而是 Rocket 的默认 404 页面。
- 没有日志记录这次打开失败（`NamedFile::open` 的错误被 `.ok()` 丢弃了），服务端无感知。

#### 对象存储：预签名地址下载

```
客户端拿元数据 → get_url() 返回 S3 预签名 URL（5 分钟有效）
    ↓
客户端直接请求 S3（不再经过 Vaultwarden）
    ↓
S3 校验预签名 → 成功返回文件 / 失败返回 S3 自身的错误码
```

**缺文件时的表现**：

- 预签名地址生成阶段不会失败——`presign_read()` 只是算签名，不检查对象是否存在。
- 客户端拿到 URL 后请求 S3，如果对象不存在，S3 返回 `NoSuchKey` 错误（HTTP 404），附带 S3 自己的 XML 错误消息。
- 错误完全由 S3 返回，Vaultwarden 根本不参与这次请求，服务端日志里没有任何记录。
- 如果预签名过期（5 分钟），S3 返回 `AccessDenied`（HTTP 403），和文件不存在是不同的错误码，客户端理论上可以区分"签名过期"和"文件没了"。

#### 对比表

| 维度 | 本地文件系统 | 对象存储 |
|------|------------|----------|
| 下载请求经过 Vaultwarden | ✅ 经过 | ❌ 不经过 |
| 生成下载地址时检查文件存在 | ❌ 不检查 | ❌ 不检查 |
| 缺文件时的 HTTP 状态码 | 404（Rocket 默认） | 404（S3 返回 `NoSuchKey`） |
| 缺文件时的错误体 | Rocket 404 页面 | S3 XML 错误消息 |
| 签名过期时的表现 | token 过期 → 404（token 校验失败返回 None） | S3 返回 403 `AccessDenied` |
| 服务端是否有日志 | ❌ 无（`.ok()` 丢弃错误） | ❌ 无（请求不经过服务端） |

### 8b.2 写入残留的形态差异

写入都走 OpenDAL 的统一接口 `save_temp_file()`，但底层行为不同：

#### 本地文件系统

- `save_temp_file()` 最终调用 OpenDAL 的 FS backend 写文件。
- FS backend 的写入是"先写临时文件、再 rename"模式（OpenDAL 内部的原子写入策略）。
- 如果写入中途失败（磁盘满、权限不足），临时文件可能残留在附件目录里，文件名通常是 OpenDAL 内部的临时命名格式，不是 `{cipher_id}/{attachment_id}` 的正常路径。
- 这种残留临时文件不会被 Vaultwarden 识别为有效附件（因为路径格式不匹配），也不会被 `Attachment::delete()` 清理（因为 delete 只删 `get_file_path()` 拼出来的路径），但会占用磁盘空间。
- 如果 FS backend 在 rename 之前就失败了，正常路径下不会有任何文件，表现和场景 A 一样。

#### 对象存储

- `save_temp_file()` 调用 OpenDAL 的 S3 backend，S3 的写入是"全部上传完成后才生成对象"的语义（multipart upload 或单次 PUT）。
- 如果上传中途失败，S3 可能留下未完成的 multipart upload，这些碎片不会以完整对象的形式出现在 bucket 里，不会影响正常路径的 GET 请求。
- 但未完成的 multipart 会占用存储空间（S3 按 uploaded parts 计费），而且不会自动过期——需要通过 S3 的 lifecycle 规则或手动清理 multipart upload 来释放。
- 如果 S3 PUT 成功但后续 `attachment.save()` 或大小校验失败，S3 上的对象已经存在，但数据库记录可能已经被场景 C 的回滚删掉了，形成另一种孤儿文件。

#### 对比表

| 维度 | 本地文件系统 | 对象存储 |
|------|------------|----------|
| 写入方式 | 临时文件 + rename | multipart upload / 单次 PUT |
| 写入中途失败的残留 | 临时文件（非正常路径） | 未完成的 multipart parts |
| 正常路径下是否有部分文件 | ❌ rename 未完成则无 | ❌ PUT 未完成则无完整对象 |
| 残留是否影响正常附件路径 | ❌ 不影响 | ❌ 不影响 |
| 残留是否占用存储空间 | ✅ 占用 | ✅ 占用（按 parts 计费） |
| 残留的自动清理 | ❌ 无（需手动删临时文件） | ⚠️ 可通过 S3 lifecycle 规则自动清理过期 multipart |

### 8b.3 删除残留的形态差异

删除都走 `Attachment::delete()` 里的 `operator.delete(&file_path)`，但底层行为不同：

#### 本地文件系统

- `operator.delete()` 调用 FS backend，直接 `std::fs::remove_file`。
- 如果文件不存在，OpenDAL 返回 `NotFound`，代码只打 debug 日志，当成功处理。
- 如果文件被其他进程占用（Windows 上常见），删除会失败，返回非 NotFound 错误，产生本地孤儿文件。
- 孤儿文件就在 `{attachments_folder}/{cipher_id}/{attachment_id}` 路径下，文件名就是 attachment_id，可直接 `ls` 找到。
- 当条目下所有附件都删完后，`{attachments_folder}/{cipher_id}/` 目录可能变成空目录，OpenDAL 的 FS backend 不会自动清理空目录。

#### 对象存储

- `operator.delete()` 调用 S3 backend，发送 DELETE 请求。
- 如果对象不存在，S3 的行为取决于版本控制配置：未开启版本控制时，删除不存在的对象返回 204（当作成功）；开启版本控制时，会插入一个删除标记。
- OpenDAL 统一将"S3 对象不存在"映射为 `NotFound`，代码处理逻辑和本地一样——打 debug 日志，当成功。
- 如果 S3 删除失败（权限不足、网络中断等），会产生 S3 上的孤儿对象。
- S3 上的孤儿对象就是 `{cipher_id}/{attachment_id}` 这个 key，没有目录层级概念，只是一段前缀。

#### 对比表

| 维度 | 本地文件系统 | 对象存储 |
|------|------------|----------|
| 删除不存在文件的行为 | `NotFound` → debug 日志 | `NotFound` → debug 日志（S3 可能返回 204） |
| 删除失败的典型原因 | 文件被占用（Windows）、权限不足 | S3 权限不足、网络中断 |
| 孤儿文件的存在形式 | 本地磁盘上的常规文件 | S3 bucket 中的对象 |
| 空目录残留 | ✅ 可能有空目录 | ❌ 无目录概念 |
| 删除失败后重试的难度 | 低（重新调 `operator.delete()`） | 低（同样重新调用） |

### 8b.4 外部清理方式差异

目前 Vaultwarden 没有内置的孤儿文件清理机制，所以只能靠外部手段。两种后端的清理难度和可用工具差别很大：

#### 本地文件系统的外部清理

1. **发现孤儿文件**：列出 `{attachments_folder}/` 下所有文件，提取 `{cipher_id}/{attachment_id}` 路径，与数据库 `attachments` 表对比。路径不在数据库里的就是孤儿文件。
2. **发现悬空记录**：反过来，查数据库里所有附件记录，对每条记录检查 `{attachments_folder}/{cipher_id}/{attachment_id}` 是否存在。文件不存在但记录还在的就是悬空记录。
3. **清理空目录**：删除条目后，`{attachments_folder}/{cipher_id}/` 可能变成空目录，可以用 `find -type d -empty` 清理。
4. **工具**：shell 脚本即可完成，不需要额外依赖。

#### 对象存储的外部清理

1. **发现孤儿对象**：列出 S3 bucket 下所有对象（S3 ListObjectsV2），提取 key 中的 `{cipher_id}/{attachment_id}` 部分，与数据库对比。这要求 S3 list 权限，且对象数量多时 list 操作有性能和费用开销。
2. **发现悬空记录**：和本地一样，查数据库后对每条记录检查 S3 对象是否存在（HEAD 请求）。对象数量多时大量 HEAD 请求会有延迟和费用。
3. **清理未完成的 multipart**：S3 的 ListMultipartUploads 可以找到所有未完成的分片上传，配合 AbortMultipartUpload 清理。也可以配置 bucket lifecycle 规则，自动中止超过指定天数的未完成 multipart。
4. **工具**：需要 AWS CLI 或 S3 SDK，比本地 shell 脚本复杂。

#### 对比表

| 维度 | 本地文件系统 | 对象存储 |
|------|------------|----------|
| 发现孤儿文件的方式 | `ls` + 数据库对比 | S3 ListObjectsV2 + 数据库对比 |
| 发现悬空记录的方式 | `test -f` 本地路径 | S3 HEAD 请求逐个检查 |
| 清理空目录/碎片 | `find -type d -empty -delete` | S3 lifecycle 规则清理过期 multipart |
| 所需权限 | 本地文件系统读写 | S3 List + Get + Delete 权限 |
| 费用 | 无 | List/HEAD/DELETE 请求有 API 调用费用 |
| 复杂度 | 低（shell 脚本） | 中（需要 S3 CLI 或 SDK） |

### 8b.5 小结：存储后端如何影响残留问题的严重程度

两个后端的数据库层行为完全一致——悬空记录和孤儿文件的判定标准相同，配额影响也相同。差异只在"发现和清理的难易程度"上：

- **本地文件系统**：残留容易发现（`ls` 就能看到），容易清理（`rm` 即可），但没有自动化；空目录残留是本地独有的小问题。
- **对象存储**：残留不容易发现（需要 list + 对比），清理需要 S3 权限和工具，但有 S3 lifecycle 这种平台级补偿手段（自动清理过期 multipart）；而且 S3 对"删除不存在的对象"更宽容（返回 204 而非报错），减少了因 NotFound 导致的误报。

如果要做一致性治理，最务实的思路是：

- 本地存储：加一个定时任务，扫目录比数据库，删孤儿文件和空目录，删数据库里的悬空记录。
- 对象存储：配置 S3 lifecycle 规则清理过期 multipart，再用类似的定时任务扫 S3 list 比数据库。

## 9. 一张心智图串起来

可以把整套逻辑理解成下面这条链：

1. 先用条目权限决定用户能不能碰附件。
2. 再用附件归属校验，确认附件确实属于这个条目。
3. 上传时重点防配额超限和 v2 大小对不上。
4. 下载时重点防 token 被拿去下载别的附件。
5. 删除时优先保证数据库别再暴露失效附件，再尽量把物理文件清掉。
6. 条目软删除保留附件，条目硬删除才真正触发附件清理。

如果后面还要继续深挖，最值得看的就是两个边界：

- 本地文件下载为什么只校验 token、不回查登录态。
- 物理文件删除失败时为什么接受"库里已删、文件残留"的状态，以及后续有没有补偿清理机制。
